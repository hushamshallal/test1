import React from 'react';

const ThemeSelector: React.FC = () => {
  // Logic for theme switching can be added here later
  return (
    <button 
      className="absolute top-4 right-4 bg-slate-700 text-white p-2 rounded-lg text-sm hover:bg-slate-600 transition-colors"
      title="Theme selection coming soon!"
    >
      الثيم
    </button>
  );
};

export default ThemeSelector;